# AgentExecTrace project-scoped Codex defaults
# Migrated from E:\project_AD9361\.codex\config.toml on 2026-08-20.
# Keep user-level trust, marketplace, plugin, and hook state in ~/.codex/config.toml.

# Everyday implementation, debugging, and validation.
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"

# Normal /review stays on Terra to avoid making every review a Sol-cost event.
# Sol is used only as an explicit one-off escalation under AGENTS.md policy.
review_model = "gpt-5.6-terra"
